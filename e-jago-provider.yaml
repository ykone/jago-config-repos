spring:
  application:
    name: e-jago-provider
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:postgresql://localhost:5432/ejago_provider_db
    username: dev
    password: dev
    hikari:
      poolName: Hikari

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

server:
  port: 8091